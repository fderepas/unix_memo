The [Rootless mode](https://docs.docker.com/engine/security/rootless/) describes running the Docker daemon and containers as a non-root user to mitigate potential vulnerabilities in the daemon and the container runtime.

